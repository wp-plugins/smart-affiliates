/*global Backbone */
var App = App || {};

(function() {
    'use strict';

    // Person Model
    App.Models.Payment = Backbone.Model.extend({
        defaults: {
            
        }
    });
    
})();
